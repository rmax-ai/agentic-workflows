# Current Plan

## Iteration focus

Iteration `20260321-092906` is scoped to `optimize-adapt` as the next dependency-safe `critical` risk refinement. The target batch is one new canonical optimization pattern plus a few linked grounded instances if the work can stay bounded at severe retuning, protected-priority adaptation, or governed optimization-state recommendation without drifting into planning, authority selection, collaboration ownership, or direct execution.

## Current phase

- Phase 2: controlled vocabularies are complete and stable enough for bounded Phase 7 refinement.
- Phase 3: navigation views are complete and should remain derived from canonical pattern inventory.
- Phase 4: top-level family overview docs are complete under `docs/patterns/`.
- Phase 5: canonical coverage spans thirty-six patterns across all nine top-level pattern families, and every top-level family now has at least three canonical patterns while `transform-process` and `recommend-decide-escalate` sit at five, `gather-retrieve-synthesize`, `investigate-reconcile-verify`, `monitor-detect-triage`, `plan-coordinate-schedule`, and `execute-automate` sit at four, and the remaining families sit at three.
- Phase 6: grounded instances cover all nine families across all seven currently modeled domains, with one hundred twenty-three instance files now committed.
- Phase 7: coverage refinement remains active, and `recommend-decide-escalate` now joins `gather-retrieve-synthesize`, `transform-process`, `investigate-reconcile-verify`, `monitor-detect-triage`, `plan-coordinate-schedule`, and `execute-automate` in spanning the full tracked risk ladder.
- Tooling baseline: `uv`-managed Python 3.14 helper workflow remains the validation path.

## Ordered tasks

1. Confirm the `optimize-adapt` family can absorb one `critical` risk anchor while preserving a clean family boundary around adaptation and governed retuning.
2. Add one new canonical `optimize-adapt` pattern and a small set of linked instances only if the outputs remain optimization-state updates, queue-policy retuning, or governed adaptation packages rather than plans, decisions, or executed changes.
3. Synchronize only the directly affected family doc and derived browse views with the new canonical inventory.
4. Validate repository YAML with `uv run python scripts/python/validate_yaml.py` after the content batch, then update `.agent/` execution memory to reflect the new coverage state and next gap.

## Iteration checkpoint

- Timestamp: `20260321-092906`
- In-flight scope: evaluate and, if valid, land one bounded `critical`-risk `optimize-adapt` anchor plus a few linked instances and the directly affected derived references.
- Coverage hypothesis: a successful batch would move `optimize-adapt` from low/moderate/high coverage to full low/moderate/high/critical coverage without reopening broader domain-balancing work.
- Reflection placeholder: capture whether the family boundary stayed clean and whether any new durable loop or tooling proposal emerged only after the batch is complete.
- Planned orchestrator follow-up: validate YAML, refresh `.agent/` status and coverage files, write the iteration log, and then queue the remaining narrow `critical` gap in `human-agent-collaborative-work` if still untouched.

## Constraints

- Keep the ontology pattern-first: fill the structural `critical`-risk gap in `optimize-adapt`, not an arbitrary domain-example gap.
- Ensure every subagent task ends with exactly one git commit and leaves no unrelated file churn behind.
- Prefer one modest batch with one new canonical pattern and a few instances over broader family expansion.
- Keep governance, reversibility, privacy, and auditability explicit in every new pattern and instance.
- Keep critical optimization work centered on retuning, protected-priority adaptation, governed optimization bundles, or severe policy-bounded reprioritization rather than command sequencing, authority recommendation, collaborative memo drafting, or direct execution.
- If the family boundary cannot be kept clean, stop at documenting that conclusion in `.agent/` memory rather than forcing a pattern that belongs elsewhere.

## Expected outcome

This iteration should either leave `optimize-adapt` with representative `critical` risk coverage through one bounded severe-retuning anchor and a few grounded examples, or explicitly document why that gap should remain open for a later family if the boundary proves incoherent.
