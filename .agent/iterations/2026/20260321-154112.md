# Iteration 20260321-154112

## What changed

- Added `mkdocs.yml` so the repository can publish a derived documentation site from `build/site-docs` into `build/site` with MkDocs Material.
- Added `scripts/python/build_site_docs.py` to regenerate a presentation-oriented documentation tree from canonical Markdown, YAML patterns, browse views, vocabularies, instances, and the JSON Schema.
- Updated `pyproject.toml` and `README.md` so the publication workflow is part of the declared Python tooling and documented local commands.
- Refreshed `.agent/decisions.md`, `.agent/current-plan.md`, `.agent/ontology-status.yaml`, and `.agent/repo-map.md` so repository memory records the site as a derived publishing layer rather than a parallel authoring surface.

## Validation

- `uv lock` succeeded and refreshed the declared Python helper dependency set.
- `uv run python scripts/python/build_site_docs.py` succeeded and regenerated `build/site-docs` from canonical repository sources.
- `uv run mkdocs build` succeeded and rendered the static site into `build/site`.
- `uv run python scripts/python/validate_yaml.py` still passed after the publication-tooling batch.

## Reflection

This iteration stayed dependency-safe because it did not move or redefine canonical ontology content. The new site layer is valid only so long as it keeps navigation and reader-facing rendering derived from repository truth in `README.md`, `docs/`, `data/`, `instances/`, and `schema/` rather than becoming a second documentation tree that authors edit by hand.

## Next step

Run the publication and validation commands locally, fix any generator or link issues that surface, and then return to the tied-domain refinement track unless publication gaps still need a bounded follow-up.
