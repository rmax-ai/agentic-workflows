# Iteration 20260322-000318

## Summary

- Completed the `.agent` memory-structure cleanup by
  turning `.agent/repo-map.md` into a compact read-first
  index and moving bulky structural detail into four files
  under `.agent/repo-map/`.
- Recorded the durable rule in `D-0015` and aligned
  `AGENTS.md`, `.agent/README.md`, `.agent/mission.md`,
  `.agent/backlog.yaml`, `.agent/prompts/mega-prompt.txt`,
  `.agent/prompts/operator-prompt.txt`, and `.gitignore`
  with index-first reading for both decisions and repo-map.
- Refreshed `.agent/current-plan.md` so recent execution
  memory now explicitly captures the memory-structure
  refactor while leaving the active Phase 7 content target
  unchanged.

## What changed

This iteration finished the same cleanup pattern that had already
been applied to decisions. `.agent/repo-map.md` is now a short
repository-map index that points to four predictable shards:
root and automation, execution memory, ontology assets, and
instances plus planned expansion. That keeps repo-map inside the
read-first execution-memory set without forcing every future
agent session to scan a long structural inventory before
starting work.

The supporting instruction layer was then tightened so the
expected startup workflow is explicit instead of implied:
read `.agent/decisions.md` first and open `D-code-slug`
detail files only when the rationale matters, and read
`.agent/repo-map.md` first and open `.agent/repo-map/*.md`
only when the task needs deeper structural detail. The new
durable rule was recorded in
`.agent/decisions/D-0015-shard-repo-map-into-an-index-and-detail-files.md`,
and `.gitignore` plus `.agent/backlog.yaml` were updated so
the new shard directory is both tracked and treated as part
of the committed execution-memory bootstrap.

## Validation

- Reviewed the current repository state before editing,
  including `.agent/current-plan.md`, recent iteration
  history, and git working-tree status.
- Verified that `.agent/repo-map.md` is now compact, that
  `.agent/repo-map/` contains the intended four detail files,
  and that the read-first guidance is internally consistent
  across the updated agent docs and prompts.
- No ontology, schema, vocabulary, pattern, instance, or
  derived-site artifacts changed in this continuation step,
  so the YAML/site build pipeline was not rerun.

## Reflection

Read-first execution memory only stays useful if its entry points
remain fast to scan. This cleanup reinforces that principle
symmetrically across decisions and repo-map: concise indexes
handle startup orientation, while detailed rationale or
structural inventory moves into predictable on-demand shards.
That should reduce prompt load and make future memory edits
more targeted and less conflict-prone.

No new proposal was added this iteration because the refactor
itself already captured the durable rule through D-0015 and
brought the prompt and documentation layer into alignment.

## Next step

Return to the active Phase 7 content plan: select one
already-canonical, structurally neutral, high-governance or
approval-bound slice in a trailing domain and add exactly one
new grounded instance unless that example exposes genuine
canonical or browse drift.
