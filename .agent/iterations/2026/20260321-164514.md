# Iteration 20260321-164514

## What changed

- Added `.github/workflows/publish-site.yml` so pushes to `main` and manual workflow dispatches can validate the repository, regenerate the derived documentation tree, build the MkDocs site, and publish `build/site` through GitHub Pages.
- Updated `README.md` so the deployment path and the GitHub Pages prerequisite are documented alongside the local publication commands.
- Refreshed `.agent/decisions.md`, `.agent/current-plan.md`, `.agent/ontology-status.yaml`, and `.agent/repo-map.md` so repository memory treats deployment automation as part of the same derived publication layer.

## Validation

- Workflow structure should be validated by GitHub Actions on first run after GitHub Pages is configured to use GitHub Actions as its source.
- The deployment workflow intentionally reuses the already-validated local commands: `uv run python scripts/python/validate_yaml.py`, `uv run python scripts/python/build_site_docs.py`, and `uv run mkdocs build`.

## Reflection

This iteration stayed dependency-safe because it did not add a second build path. Deployment remains coherent only while GitHub Actions publishes exactly the `build/site` output produced by the repository's canonical local pipeline, with no hand-maintained Pages content or committed generated HTML.

## Next step

Enable GitHub Pages to use GitHub Actions in repository settings, run the workflow once, and then return to the tied-domain refinement track unless deployment exposes a bounded CI or permissions gap.
