# Iteration 20260321-020031

## What changed

- Scoped the batch in `.agent/current-plan.md` and committed that plan update before any new content work.
- Added `instances/finance/treasury-cash-position-discrepancy-investigation.md` as a finance investigation example linked to `incident-root-cause-analysis`.
- Added `instances/compliance/sanctions-screening-gap-root-cause-investigation.md` as a compliance investigation example linked to `incident-root-cause-analysis`.
- Updated `.agent/ontology-status.yaml`, `.agent/coverage-matrix.yaml`, `.agent/backlog.yaml`, `.agent/repo-map.md`, and `.agent/current-plan.md` to reflect the stronger `investigate-reconcile-verify` grounding and to retarget the next batch toward `gather-retrieve-synthesize`.

## What was learned

- `incident-root-cause-analysis` transfers cleanly into finance and compliance when the instance centers on evidence reconciliation, timeline normalization, and controlled uncertainty rather than alert triage or policy recommendation.
- Investigation examples become more structurally useful when they preserve the distinction between observed control or reconciliation gaps and inferred causes that still require human sign-off.
- After this batch, `gather-retrieve-synthesize` looks thinner than `investigate-reconcile-verify`, so the next modest grounding step should probably rebalance retrieval and synthesis coverage instead of continuing to deepen investigation immediately.

## Reflection

- Workflow quality: the scope stayed clean because the pre-work plan commit and the narrow subagent boundary made the content task easy to review.
- Prompt quality: explicitly telling the subagent not to drift into triage, recommendation, or execution produced instances that stayed inside the intended family boundary.
- Ontology coherence: the new instances broadened an existing canonical pattern instead of inventing domain-first variants, which keeps the pattern-first spine intact.
- Pattern quality: `investigate-reconcile-verify` now has materially different engineering, finance, and compliance groundings, making the family easier to understand across governed environments.
- Loop and tooling friction: no new durable issue surfaced beyond existing proposals, so no new proposal was added this iteration.

## Validation

- `uv run python scripts/python/validate_yaml.py`

## Commits in this iteration

- `5a4ff91` — `chore(agent): scope investigation grounding batch`
- `cef0920` — `feat(instances): add finance and compliance investigation grounding`

## Next step

- Re-read `research-synthesis-with-citation-verification` and the current synthesis-oriented grounding, then add one or two finance, compliance, or operations `gather-retrieve-synthesize` examples in the next modest batch.
