# Iteration 20260321-170522

## What changed

- Added `.agent/prompts/mermaid-illustration-orchestrator-prompt.txt` as a dedicated prompt for orchestrating Mermaid workflow-diagram insertion across `docs/patterns/*.md` and `instances/**/*.md`.
- The prompt instructs the orchestrator to skip files that already contain Mermaid, use one subagent per file, add exactly one diagram per changed file, validate Mermaid syntax, and keep every diagram bounded to the workflow semantics already documented in the Markdown file.
- The prompt includes Mermaid guidelines and best practices, including using double quotes for labels and `<br>` for line breaks, plus an example Mermaid block.
- Refreshed `.agent/current-plan.md` and `.agent/repo-map.md` so the new prompt is visible in repository memory.

## Validation

- Prompt validation is structural and semantic: it defines file scope, skip behavior, subagent granularity, Mermaid formatting rules, and validation requirements clearly enough to guide a future bounded batch.

## Reflection

This iteration stayed narrow and tooling-focused. The prompt should help future runs add diagrams consistently, but only if it continues to treat Mermaid as a derived explanatory layer and never as permission to invent workflow steps or collapse family boundaries.

## Next step

Use the new prompt on a small pilot batch of pattern and instance Markdown files, then verify the resulting diagrams render cleanly in the MkDocs site and stay faithful to the prose.